spring:
  profiles:
    group:
      "local": "localDB, localS3, localSecret"
      "dev": "devDB, devS3, devSecret"
      "prod": "prodDB, prodS3, prodSecret"
---

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

cloud:
  aws:
    s3:
      bucket: rikubucket
    region:
      static: ap-northeast-2
    stack:
      auto: false

---
season:
  start-date: 2026-03-02   # KST 기준